"""Business modules namespace (Phase A.5).

Each module is a package `app.modules.<name>` exposing `api:router`. Modules are
activated per deployment via `MODULES_ENABLED` (the Module Loader includes only
the enabled ones). Empty for now — modules land with the porting phases.
"""
