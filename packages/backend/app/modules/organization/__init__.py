"""organization — business entity identity + hierarchical org units (Phase A.5).

The deploying customer's entity (Organization) and its sub-entities
(OrgUnit: divisions / departments / agencies, self-referencing hierarchy).
Generic: no gov-specific fields (see docs/architecture/MODULES_ORG_LOCATION.md).
Distinct from config-store `branding.*` (app theming) — this is who the entity is.
"""
